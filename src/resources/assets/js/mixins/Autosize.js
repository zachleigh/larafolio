let autosize = require('autosize');

export default {
    components: {},

    data: function () {
        return {
            /**
             * Autosize form fields.
             */
            autosize: autosize
        }
    }
};